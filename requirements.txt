mcp>=0.9.0
httpx[http2]>=0.25.0
requests>=2.31.0
pydantic>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
uvicorn>=0.27.0
starlette>=0.36.0
sse-starlette>=1.8.0
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-ruff>=0.2.0  # Pytest plugin to run ruff checks automatically
ruff>=0.14.0  # Use same major range as CI so local venv and Docker see same rules

# Analytics dependencies
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.0  # For Alembic offline mode

# JWT support for Auth0 tokens
python-jose[cryptography]>=3.3.0
